# Comments / thread

fx/vx return counts only. Conversation list APIs failed:

- `api.fxtwitter.com/.../status/{id}` — parent tweet only, no replies array
- `api.vxtwitter.com` — reply *count* (163), no thread
- `r.jina.ai` on `x.com` — AbuseAlleviationError (blocked until 2026-09-02T18:55:15Z)
- `r.jina.ai` on `http://fixupx.com/pierreeliottlal/status/2094892848042725416` and `vxtwitter.com` — HTTP 403, post body only, no replies
- Threadreader — login wall
- xcancel — shut down

No author self-replies in the fxtwitter payload (`replying_to` null). Tree text is the note-tweet in `post.md`.

163 replies at capture. Post asks people to comment **“BOT”** so the author can DM a link — lead magnet, not a public thread dump. Remaining replies behind X login / official conversation API.

## Repo (already in the library; folder not rewritten)

- **https://github.com/romangojiberryAI/gojiberryai-sales-os** — item `github-romangojiberryAI-gojiberryai-sales-os`. MIT, **70★** / 20 forks at this capture (GitHub API; existing `source.json` still has 69). Created 2026-09-01. Grok/Claude plugin `sales-os` + markdown agents + hosted MCP `https://mcp.gojiberry.ai/mcp` (HEAD 200 `application/json`).
- Brand recap the next morning: item `x-2095081419202560010` (https://x.com/gojiberryai/status/2095081419202560010), 87 “BOT” replies, same funnel.
