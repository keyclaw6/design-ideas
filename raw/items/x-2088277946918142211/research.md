# Research

## What it is
RobitOverload (@10_X_eng) VibeCAD progress: exploded views and simulations of a mechanical assembly, claimed “decent and very usable,” with the prompt promised below the (unharvested) video.

## How it works
- VibeCAD = prompt-to-CAD with assembly visualization (explode, simulate), not just a single solid.
- Capture missed the actual prompt (fxtwitter retry; no media files). The reusable idea is: exploded views + motion sims as the acceptance test for agent CAD.
- Same job as grill-me+OpenSCAD and CadX, different UI (simulation-first).
- For keyboards/enclosures, exploded views are how you verify fastener stacks before print.

## Why saved
Hardware lane needs CAD agents that can explode and simulate, not only generate a pretty STL. This is a capability bookmark; prompt may need a re-fetch.

## Topics
`agent-skills`, `keyboard-pcb`

## Related
- `x-2088252062454751483` — OpenSCAD + grill-me (local parametric)
- `x-2088296314484162719` — Fusion as a queryable CAD database
- `x-2088308976278790258` — CadX hexacopter text-to-CAD
- `x-2087272209429766596` — CadX parametric bookshelf

## Use when
Evaluating prompt-to-CAD tools that must show exploded assemblies / motion, or comparing VibeCAD vs CadX vs OpenSCAD for hardware.
