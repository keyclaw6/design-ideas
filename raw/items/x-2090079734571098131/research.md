## What it is

Designer showing an alternate visual direction for an AI SaaS, attributed to custom skills + a personal video tool + Claude (no public Figma).

## How it works

- Pipeline, not a product: skills (taste/layout) → Claude implementation → video tool for motion/hero.
- No repo URL; the keepable content is the workflow split, same shape as elayadesign's Seedance+Composer post.
- Useful as a 'direction exploration' example: multiple looks from the same skill stack.
- Treat attached media (if present in media/) as the visual reference, not as a component library.

## Why saved

KB's BESS landings will need several directions fast; this is the skills+video+LLM pattern without a named SaaS lock-in.

## Topics

`design`, `agent-skills`, `video-generation`

## Related

`github-nexu-io-open-design`, `x-2089770081459056765`, `web-open-design-ai`, `github-leonxlnx-taste-skill`

## Use when

Exploring alternate marketing-page directions with skills + Claude + a video model, or citing a real workflow for vibe-design + motion.
