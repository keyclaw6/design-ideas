## What it actually does

Agent command suite + CLI detectors for frontend quality. PRODUCT.md/DESIGN.md split mirrors Google DESIGN.md pattern. Browser extension for live iteration.

## Design / BESS marketing site

Run `/impeccable init` for BESS product context, then `craft`/`polish`/`audit` on marketing pages. Detectors catch a11y and generic-AI tells.

## 3D / agents / SEO

**agent-skills**, **design**: agent workflow tool. `audit` covers a11y/responsive (SEO-adjacent). Not 3D-focused.
