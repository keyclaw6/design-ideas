# KERNEL browsers now support custom proxies with CA bundles. pass your CA bundle in when you create the proxy and it will

@usekernel

KERNEL browsers now support custom proxies with CA bundles.

pass your CA bundle in when you create the proxy and it will be installed in the browser's trust store automatically.
