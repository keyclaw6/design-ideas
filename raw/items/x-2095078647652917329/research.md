## What they are actually doing

A **roundup tweet** asking for DESIGN.md sources for coding agents. Nine URLs in the note-tweet; one extra from the only visible first-level comment (Sokosumi generator). The 4096² PNG is a collage of the list, not extra links.

Library already has related items: `github-google-labs-code-design-md` (the spec), `github-nexu-io-open-design` (open-design.ai). This post is a directory, not a new spec.

**Sites (light HTTP/jina only)**

- **getdesign.md** — catalog of 550+ DESIGN.md analyses + private briefs + a website starter kit; claims Google DESIGN.md spec. Request/catalog CTAs (`/request`, `/design-md`).
- **Sokosumi** `/tools/design-md` — “free DESIGN.md generator for AI coding agents” on an “AI Coworkers” marketplace.
- Others not opened beyond the tweet facets: Refero styles, designmd.me, open-design.ai, designmd.supply, aura.build, neuform.ai, design-md.hyperbrowser.ai, typeui.sh.

## Open questions

- Full comment-thread link dump (~27 replies + 4 quotes still login-walled). The parent explicitly asked “What am I missing?”
- How each site maps to the Google DESIGN.md schema vs a proprietary prompt pack.
